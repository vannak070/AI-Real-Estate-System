# Update: <short title>

`<name>`: <same slug as the feature this revises>
Against: `claude/features/<name>.md` (+ commit / PR if known)

## What was produced

<Briefly, the current state of the output being changed.>

## What's wrong / what to change

1. <precise change — "the badge shows the agent id, it should show the name">
2. <...>

## Constraints

- Do **not** regress: <behaviour that must still pass>
- Keep the diff to: <files / modules>
- Boundary rules in `claude/config.md` §3 still apply.

## Files likely involved

- `<path>` — <what changes>

## Acceptance

- [ ] <observable result 1>
- [ ] Existing `claude/tests/test-<name>.md` still passes
